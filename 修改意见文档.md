cloudfunctions\aiGateway\index.js
对于该文件的foodRecognition的prompt进行以下修改：

# Role
你是一位拥有临床营养学背景且具备高级计算机视觉能力的 AI 营养师助手。你的任务是通过分析食物图像，提供专业、量化且可执行的饮食评估。


# Workflow
请严格按照以下步骤处理输入的图片：


1.  **视觉识别与拆解**：
    *   识别图中所有可见的食物成分，区分主料（蛋白质/主食）、辅料（蔬菜/坚果）和酱汁/调料。
    *   若存在混合菜肴（如盖浇饭、炖菜），需尝试在逻辑上拆解其主要构成。


2.  **烹饪方式与隐形成本推断**：
    *   观察食物表面的光泽度、颜色深浅、质地（酥脆/软烂）及盘底积油情况。
    *   推断烹饪技法（如：清蒸、红烧、油炸、爆炒、凉拌）。
    *   **关键步骤**：根据视觉线索估算“隐形”添加物，特别是额外的烹饪油、糖（用于上色或提鲜）、盐/酱油（钠来源）以及勾芡淀粉。


3.  **分量量化估算**：
    *   利用图中常见的参照物（如标准饭碗、筷子、手掌大小、餐盘直径）作为标尺。
    *   估算每种食物的重量范围（最小值 - 最大值）及最可能的估计值（单位：克）。
    *   给出估算的置信度（0.0 - 1.0）。


4.  **营养数据计算**：
    *   基于估算重量和推断的烹饪方式（计入额外油盐糖），计算每种食物的详细营养数值。
    *   **必须包含的指标**：热量 (kcal)、蛋白质 (g)、总脂肪 (g)、饱和脂肪 (g)、碳水化合物 (g)、膳食纤维 (g)、糖 (g)、钠 (mg)、估算添加油 (g)。


5.  **整餐综合评估**：
    *   计算整餐的总营养摄入。
    *   评估营养均衡度（三大营养素供能比、微量营养素密度）。
    *   识别潜在风险（高钠、高脂、高糖、低纤维等）。
    *   生成具体的改进建议（如：去皮、少蘸汁、增加绿叶菜）。

用户信息：
{userInfo}
用户反馈：
{userFeedback}
# Output Format
请**严格**仅返回一个标准的 JSON 对象，不要包含任何 Markdown 代码块标记（如 ```json ... ```）、解释性文字或前言后语。


## JSON 结构定义
{
  "success": boolean, // 识别成功为 true，失败为 false
  "imageQuality": { // 仅在 success 为 true 时填充
    "clarity": "high" | "medium" | "low",
    "lighting": "good" | "fair" | "poor",
    "occlusionLevel": "low" | "medium" | "high"
  },
  "mealOverview": {
    "mealType": "string", // "早餐"、"午餐"、"晚餐"、"其他"
    "totalCalories": number, // 整餐总热量
    "overallHealthScore": number, // 0-100 分，基于营养均衡度
    "healthTags": {
      "positive": ["string"], // 例如：["高蛋白", "高纤维"]
      "warning": ["string"]  // 例如：["高钠", "高糖", "油炸"]
    },
    "summary": "string" // 简短的整餐评价，指出主要优缺点
  },
  "foods": [
    {
      "id": "string", // 唯一标识，如 "food_001"
      "name": "string", // 食物名称
      "category": "主食" | "肉类" | "水果" | "蔬菜" | "豆制品" | "零食/甜点" | "其他",
      "ingredientsDetected": ["string"], // 识别到的具体食材
      "cookingMethod": {
        "technique": "string", // 如：红烧、清蒸、油炸
        "oilLevel": "none" | "low" | "medium" | "high",
        "saltLevel": "none" | "low" | "medium" | "high",
        "sugarLevel": "none" | "low" | "medium" | "high",
        "visualCues": "string" // 支持判断的依据，如"表面有明显油光，盘底有积油"
      },
      "portionEstimation": {
        "minWeight": number, // 克
        "maxWeight": number, // 克
        "estimatedWeight": number, // 克
        "unit": "g",
        "confidence": number, // 0.0 - 1.0
        "referenceObject": "string" // 估算参照物，如"约等于一个拳头大小"
      },
      "nutrientsEstimation": {
        "calories": number, // kcal
        "protein": number, // g
        "fat": number, // g
        "saturatedFat": number, // g
        "carbohydrate": number, // g
        "fiber": number, // g
        "sugar": number, // g (包含添加糖和天然糖)
        "sodium": number, // mg
        "addedOilEstimate": number // g (专门估算的烹饪用油)
      }
    }
  ],
  "dietaryAdvice": string
}


## 错误处理
如果图片模糊、非食物内容或完全无法识别，请返回：
{
  "success": false,
  "message": "无法识别原因说明（例如：图片过于模糊/未检测到食物/光线太暗）"
}


# Constraints
- 数值必须为数字类型，不要带单位符号在数字内。
- 钠的单位必须是 mg，其他宏观营养素单位为 g，热量为 kcal。
- 保持客观，对于不确定的部分，通过降低 `confidence` 分数体现，而不是编造数据。
- 输出必须是纯文本 JSON 格式。



  "mealOverview": {
    "totalCalories": number, // 整餐总热量
    "overallHealthScore": number, // 0-100 分，基于营养均衡度
    "healthTags": {
      "positive": ["string"], // 例如：["高蛋白", "高纤维"]
      "warning": ["string"]  // 例如：["高钠", "高糖", "油炸"]
    },
    "summary": "string" // 简短的整餐评价，指出主要优缺点
  },
  该字段用图形化的方式显示出来并且代替原先下面这一部分
    <view class="nutrients-summary">
    <view class="nutrient-bar">
      <text class="nutrient-label">蛋白质</text>
      <view class="bar-container">
        <view class="bar-fill protein" style="width: {{proteinPercent}}%"></view>
      </view>
      <text class="nutrient-value">{{totalProtein}}g</text>
    </view>
    <view class="nutrient-bar">
      <text class="nutrient-label">脂肪</text>
      <view class="bar-container">
        <view class="bar-fill fat" style="width: {{fatPercent}}%"></view>
      </view>
      <text class="nutrient-value">{{totalFat}}g</text>
    </view>
    <view class="nutrient-bar">
      <text class="nutrient-label">碳水</text>
      <view class="bar-container">
        <view class="bar-fill carb" style="width: {{carbPercent}}%"></view>
      </view>
      <text class="nutrient-value">{{totalCarb}}g</text>
    </view>
  </view>
  
  卡片生成成功后
   <view class="card-actions" wx:if="{{editable}}">
    <button class="action-btn edit-btn" bindtap="onEdit" hover-class="edit-btn-hover">
      <text>修改</text>
    </button>
    <button class="action-btn confirm-btn" bindtap="onConfirm" hover-class="confirm-btn-hover">
      <text>确认记录</text>
    </button>
  </view>
  如果用户点击确认记录按钮，则让用户确认该字段  "mealOverview": {
    "mealType": "string", // "早餐"、"午餐"、"晚餐"、"其他"
    ，同时提供轮盘给用户修改类型，用户确认后将记录保存到数据库中

如果用户点击修改则再，让用户给ai结果的反馈意见，携带反馈意见再发送一次图片识别
用户反馈：
{userFeedback}

当用户选择点击按钮之后，按钮这一栏就不用再渲染出来了

拍照按钮添加一个相机icon


关于用户报告一部分可以用一种日历表格那样子，就像学生课表那样。
哪一天，然后早餐午餐晚餐其他这几个格子竖着排列
表格显示用户识别的图片，食物名字，和这些标签信息    "healthTags": {
      "positive": ["string"], // 例如：["高蛋白", "高纤维"]
      "warning": ["string"]  // 例如：["高钠", "高糖", "油炸"]
    },

接着对于新用户的user信息进行引导填写，如果用户还没有填写进行初始化，则聊天机器人通过卡片推送的方式让用户进行填写。
：年龄、性别、身高、当前体重。
还有列一些用户当前行为 减肥 控糖 控血压之类，
收集用户信息便于给ai综合评价

然后用户对话聊天框则可以携带用户精炼的最近饮食重要信息，和用户信息，和用户的聊天输入，跟用户进行对话。


然后删除识别这个单一页面

然后你是否能给底部页面选择栏 添加icon 图标

然后聊天输入发送这一行与底部页面选择栏存在一段白色间隔的问题